// DTOs, DockFlowPresetsAPI and DockFlowPresetsClient are generated from Contract.talk.json.
// There is deliberately no dependency on the DockFlow app or its implementation.
public enum DockFlowRouting {
    /// Public routing hint only. Pairing does not verify the app's publisher.
    public static let providerBundleID = "com.appit.DockFlow"
}
